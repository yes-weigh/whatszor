{
    "extends": "next/core-web-vitals",
    "rules": {
        "react/forbid-dom-props": "off",

        /* False-positive: fires on any function prop received by a 'use client'
           component even when the caller is also 'use client'. All callers of
           components that receive callback props in this project are client
           components, so there is no actual serialization boundary violation. */
        "@next/next/no-unsupported-functions-in-client-components": "off",

        "no-restricted-syntax": [
            "error",
            {
                "selector": "MemberExpression[object.property.name='data'][property.name='data']",
                "message": "[API Contract] Accessing .data.data is a double-unwrap bug. The Axios interceptor in lib/api.ts already unwraps { success, data } -> payload. Access .data directly. The ONLY valid exception is the token refresh in lib/api.ts which uses raw axios and is explicitly documented."
            }
        ]
    }
}